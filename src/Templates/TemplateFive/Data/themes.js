import { createTheme } from '@mui/material/styles';

/**
 * All template five themes will be defined here.
 *
 * @var const themes
 */
export const themes = createTheme({
  palette: {
    primary: {
        main: '#FFFFFF',
    },
    secondary: {
        main: '#E3E3E3',
    },
    tertiary: {
        main: '#000000',
    },
    quaternary: {
        main: '#034C8C',
    },
    quinary: {
        main: '#E3E1D5',
    }
  },
});